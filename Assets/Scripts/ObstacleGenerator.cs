using UnityEngine;

//Will be used to generate obstacles on newly created platforms
public class ObstacleGenerator : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        
    }
}